---
# Inventaire AKILIYA — Serveurs de déploiement
# Ajouter un nouveau VPS : dupliquer le bloc host et adapter

all:
  children:

    production:
      hosts:
        vps-psm-sih:
          ansible_host: 45.67.217.48
          ansible_port: 22
          ansible_user: akiliya
          ansible_ssh_private_key_file: ~/.ssh/id_rsa_psm_sih
          ansible_python_interpreter: /usr/bin/python3
          # Méta — pas utilisé par Ansible, documentation
          location: "OVH — Roubaix, FR"
          purpose: "prod + staging AkiText"

    # Futur serveur staging dédié (décommenter quand nécessaire)
    # staging:
    #   hosts:
    #     vps-staging:
    #       ansible_host: x.x.x.x
    #       ansible_user: akiliya
    #       ansible_ssh_private_key_file: ~/.ssh/id_rsa_staging
